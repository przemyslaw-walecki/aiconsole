# Standalone MCP Server-Client

Small MCP Client-Server service designed for outsorcing python code execution.
Utilizes both MCP Server and Client to perform code on a pooled amount of workers.

## How to use

`poetry install && poetry run mcp-start`

## Tests

Afer starting the MCP service:

`poetry run test`
